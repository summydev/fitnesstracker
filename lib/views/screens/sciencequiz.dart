import 'package:flutter/material.dart';

class ScienceQuiz extends StatelessWidget {
  const ScienceQuiz({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('which of you is lorem ipsum?'),
        Column(
          children: [
          
          
                      ],
        )
      ]
    );
  }
}